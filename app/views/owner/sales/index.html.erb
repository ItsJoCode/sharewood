<h1>Mes ventes :</h1>
<br>

<div class="sales-state">
    <h2>En cours</h2>
  <div class="sales-encours">
    <ul>
      <% @sales.each do |sale| %>
        <li><%= render "shared/product_card" %></li>
      <% end %>
    </ul>
  </div>
    <h2>Validées</h2>
  <div class="sales-validées">
    <ul>
      <% @sales.each do |sale| %>
        <li><%= render "shared/product_card" %></li>
      <% end %>
    </ul>
  </div>

    <h2>Livrées</h2>
  <div class="sales-livrées">
    <ul>
      <% @sales.each do |sale| %>
        <li><%= render "shared/product_card" %></li>
      <% end %>
    </ul>
  </div>

    <h2>Annulées</h2>
  <div class="sales-annulées">
    <ul>
      <% @sales.each do |sale| %>
        <li><%= render "shared/product_card" %></li>
      <% end %>
    </ul>
  </div>
</div>

<% unless @sales.empty? %>
  <div class="geo-map"
    data-controller="map"
    data-map-markers-value="<%= @markers.to_json %>"
    data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
  </div>
<% end %>
